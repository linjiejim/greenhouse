{
  "name": "@greenhouse/sandbox-runner",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Mission Sandbox Runner — drives a Pi coding-agent session inside the isolated runtime container and streams events back to the control plane",
  "engines": {
    "node": ">=22.19.0"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json"
  },
  "dependencies": {
    "@earendil-works/pi-coding-agent": "0.83.0",
    "typebox": "1.3.7"
  },
  "devDependencies": {
    "@greenhouse/types": "workspace:*"
  }
}
