/**
 * Shared NDJSON stream reader.
 *
 * Re-exports from shared types — canonical definition lives in types/api.ts
 * so it can be reused by React Native and other clients.
 */
export { readNdjsonStream } from '@greenhouse/types/api';
