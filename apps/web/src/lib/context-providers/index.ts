/**
 * Context Providers — 统一注册入口
 *
 * 导入此文件即自动注册所有内置的上下文提供者。
 * 新增页面类型时，只需在此添加一行 import。
 */

import './chat';
import './history';
import './feature-request-list';
import './project-list';
import './project-detail';
