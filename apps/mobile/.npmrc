# Expo + pnpm needs a flat node_modules so Babel/Metro can resolve presets
# (babel-preset-expo, @react-native/babel-preset, …) and native autolinking can
# find package android/ dirs from the project root. Without this the dev-build
# Metro bundler fails with "Cannot find module 'babel-preset-expo'".
node-linker=hoisted
