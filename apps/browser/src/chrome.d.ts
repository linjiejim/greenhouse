// Pull in the chrome.* extension API types. A triple-slash reference (instead
// of tsconfig "types") resolves relative to this file, so the repo-root
// `pnpm typecheck` finds apps/browser/node_modules/@types/chrome too.
/// <reference types="chrome" />
